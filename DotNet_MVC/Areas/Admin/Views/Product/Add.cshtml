@model List<Products>
<div class="container">
    <partial name="notification" />
    <div class="d-flex justify-content-sm-end mb-4">

        <a asp-action="Upsert" asp-controller="Product" class="btn btn-primary d-flex">
            <i class="bi bi-plus-circle px-2"></i>
            <span>Add Products</span>
        </a>
    </div> 
    <table id="productListTb" class="table table-striped table-responsive-md  table-responsive-sm  table-responsive-lg " style="width:100%">
        <thead>
            <tr>
                <th>ID</th>
                <th>Product Name</th>
                <th>Description</th>
                <th>Price</th>
                <th>Category</th>
                <th>Action</th>
            </tr>
        </thead>
      <!-----------------------------
          <tbody>
            @foreach (var product in Model.OrderBy(x => x.Id))
            {
                <tr>
                    <td>@product.Id</td>
                    <td>@product.Name</td>
                    <td>@Html.Raw(System.Web.HttpUtility.HtmlDecode(product.Description))</td>
                    <td>@product.Price</td>

                    <td>@product.Category.Name </td>
                    <td>
                       
                    </td>
                </tr>
            } 
          </tbody>-->
    </table>
</div>
@section Scripts{
    <script src="~/js/handletable.js"></script>
}